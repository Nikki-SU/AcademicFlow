name: MinerU Connectivity Test

on:
  repository_dispatch:
    types: [mineru_connectivity_test]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    permissions:
      contents: read

    steps:
      - name: Test MinerU connectivity
        env:
          MINERU_API_TOKEN: ${{ secrets.MINERU_API_TOKEN }}
        run: |
          node .github/scripts/mineru-test.mjs

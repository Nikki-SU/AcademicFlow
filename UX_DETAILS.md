# 交互细节清单

> 这里记的不是「功能」，而是**做与不做都不算错、但做了明显更顺手**的地方。
>
> 每条的写法都是「现象 → 为什么 → 怎么做」，与具体技术栈无关。
> 换项目时按部件对号入座即可，不必照抄实现。

---

## 小容器：侧栏 / 模态框 / 抽屉

**原则：尽量不要出现滚动条。**

- 小容器一旦要上下滚动，用户就失去了「一眼看全」的能力 —— 看不见的选项等于不存在。
- 判断标准：容器宽度小于屏宽 1/3 时，内容区高度应在一屏内放下。
- 做法：
  - 把「专业但低频」的选项收进二级 tab 或折叠区，别和主流程平铺在一起。
  - 同一功能不要在小容器里堆两个入口（比如既给「搜索」又给「选择」，还都铺在首页）。
  - 列表类内容允许滚动（滚动列表是符合直觉的），但**表单、按钮组、选项组不允许**。

**去掉那些「只是为了对称」的东西。** 一个侧栏里放着三四个功能区块，其中两个其实可以从别处进入，就删掉两个 —— 侧栏的每多一行，都在挤压真正高频的那一行。

**宽度按信息量给，不按「留白好看」给。** 一个只放 4 个状态点的监控侧栏占了屏幕四分之一，就会把主列表挤到必须横向滚动 —— 用户每一行都要拖一次，成本远高于侧栏的观感。

- 侧栏宽度按**内容本身需要的宽度**定。4 个约定俗成的状态点，一条几字到十几字的宽度就够。
- **主内容区不允许横向滚动**是硬约束；拿不准时优先压侧栏。
- **没用到的功能不要留位置**：某个 tab / 区块（比如一条没人看的 trace 链路）实际上从没用过，就删掉，不要为了「以后可能用得上」占着空间。

---

## 编辑器

### 选区要能连续操作（高频痛点）

**现象**：选中一段文字 → 点加粗 → 这段文字不再被选中 → 想再点斜体得重新选一遍。

**为什么**：工具栏按钮的默认行为会把焦点从编辑器抢走，选区随之丢失。

**怎么做**：在工具栏区域的 `mousedown` 阶段 `preventDefault()`，阻止焦点转移。选区留在原地，用户就可以连着加粗、斜体、取消加粗，直到主动把光标点到别处为止。

```js
// 捕获阶段拦截，且只拦工具栏
el.addEventListener('mousedown', (e) => {
  if (e.target.closest('.toolbar')) e.preventDefault()
}, true)
```

这条几乎零成本，但体验差别极大 —— 它是「这个编辑器好不好用」的第一印象。

### 不要放与业务语义冲突的内置按钮

通用编辑器自带的按钮里，总有几个在你的业务里另有含义。**留着它们不是中性行为，而是误导**。

例子：某个写作工具用 `[@doi:xxx]` 标记文献引用，而 markdown 编辑器自带「引用块」（blockquote）按钮。用户看到两个都叫「引用」的东西，会以为是一回事。

做法：删掉内置的那个，或者换成自己的实现。宁可工具栏少一个按钮，也不要留一个语义打架的。

### 富文本复制要还原语义

**现象**：选中含公式的段落复制，粘贴后公式丢了，而且公式位置的内容重复了两遍。

**为什么**：KaTeX 这类渲染器默认同时输出两套 DOM —— 可视化分支 + 隐藏的语义分支（MathML）。隐藏分支通常只是 `clip` 裁掉而非 `display:none`，**仍然在选中流里**，于是一起被复制；而可视化分支是一堆拼字形的 `<span>`，看起来就像公式「丢了」。

**怎么做**：拦截 `copy` 事件，克隆选区，把渲染出来的特殊节点换回源码，再写回剪贴板。

```js
onCopy = (e) => {
  const holder = document.createElement('div')
  holder.appendChild(selection.getRangeAt(0).cloneContents())
  holder.querySelectorAll('.katex').forEach(el => {
    const tex = el.querySelector('annotation[encoding="application/x-tex"]')?.textContent
    el.replaceWith(document.createTextNode(`$${tex}$`))
  })
  e.clipboardData.setData('text/plain', holder.textContent)
  e.preventDefault()
}
```

要点：源码往往就藏在渲染器自己的语义分支里（KaTeX 写在 `<annotation>` 中），不用另外反查原文。

### 长链接要缩短或折叠

**现象**：插图后 markdown 源码里出现一行几百 KB 的 base64，比正文长几个数量级，且没法折叠。

**为什么**：把图片转 base64 内嵌是最省事的实现，但源码彻底不可读，也会拖垮编辑器。

**怎么做**：图片落成文件，源码里只留短路径。**用「相对仓库根」的路径而不是相对当前文件的路径** —— 这样把一段内容复制到另一个文件时，图片依然能显示，不需要搬运文件。

### 列表符号要自己补回，别指望框架

**现象**：有序列表在编辑器里看着「不能用」—— 缩进了、编号却没有，只是几段缩进的文字。

**为什么**：CSS reset（Tailwind Preflight 之类）为了消除浏览器差异，会把 `ol, ul` 的 `list-style` 清成 `none`。编辑器通常只补回无序列表（因为 `•` 好认），有序列表就漏了 —— 这不是编辑器 bug，是 reset 与编辑器各自补了一半。

**怎么做**：自己按嵌套层级补全 `list-style-type`（有序：`decimal` → `lower-alpha` → `lower-roman`；无序：`disc` → `circle` → `square`），并给 `padding-left` 留出符号位。**渲染视图和编辑视图要一起补** —— 只在一边补，用户切视图时会看到列表「长出来又缩回去」。

### 长正文给个「间隔上色」开关（默认开，可关）

**现象**：自己写长稿时读串行 —— 眼睛从一行末尾折回下一行开头，经常跳错行。

**为什么**：正文是纯色块，没有可依赖的行/块边界。

**怎么做**：给**块级**子元素交替淡底（`nth-child(even)` 加一个极淡的背景，别用整行斑马纹 —— 段落实高不齐，整行斑马纹会显得很吵）。要点：

- 用 CSS 的 `nth-child`，**不要用 JS 打类名**。富文本编辑器每敲一个字都会重排 DOM，JS 标类要跟渲染抢时序，一定会有闪错色的时候；`nth-child` 是「布局算的时候才算」，永远对得上。
- 做成**设置项且默认开启**，用户不喜欢可以关。
- **同一个编辑器组件在多个页面共用同一个设置值**（比如写作页和某处的笔记栏），否则用户会疑惑「为什么这儿的开关不管那儿」。

---

## 表格

### 插入时选行列，不要塞占位文字

- 点击表格按钮 → 弹出网格，鼠标滑过就高亮「几行 × 几列」，点一下插入。
- **单元格里不要放 `foo` / `bar` 之类的示例文字**：用户每次都要先删干净才能用，这是纯粹的负担。

### 已插入的表格要能改行列

表格一旦插进去，用户必然会发现「少了一行」。此时不能只让他去手写源码。

做法：鼠标移到表格上时，在表格右上角浮出 `+行 / +列 / −行 / −列` 的小工具条。增删作用于末尾（多插了要撤掉是最常见诉求）。

**改写要基于原始文本逐行做**，不要解析后重新生成 —— 重新生成会冲掉用户自己写的对齐和单元格内空格。

---

## 代码块

- **插入前先选语言**，不要插入空围栏让用户手打语言名。
- **设置里提供「默认语言」**：记一次，之后每块都省一步。
- **必须等宽字体**。代码块和正文用同一套字体会让缩进完全对不齐，读代码的人一眼就会烦躁。
- **配色用开发者熟悉的色系**（VSCode / GitHub 深色那类）。用户对代码配色的预期来自每天的 IDE，顺手沿用最省认知成本。
- 行内代码也要和正文区分开（等宽 + 淡底色）。注意：如果编辑器把公式源码也标记成 `<code>`，选择器要把它排除掉，否则公式会被渲染成代码的样子。

---

## 公式 / 结构化内容

- 让公式这类**结构化单元保持完整**：复用、插入都以「一条」为单位，不要拆成文本片段。
- 复用入口要「点一下就能用」。如果复用列表在 A 栏、确认按钮在 B 栏，用户每次都要来回跳，那就等于没做。
- 搜索 + 列表滚动是允许的（用户对列表滚动的容忍度高），但**别把搜索和选择塞进同一屏的上下两块**。

---

## 批注 / 标注

### 一条批注只该有三样东西：颜色框、原文、你的话

**现象**：一条批注卡里堆着「第 12 段」「黄色批注」「2 天前」和各种标签 —— 用户真正要看的两句话被埋在元数据里。

**为什么**：这些信息在写代码时容易拿到，就顺手放上去了。但用户扫批注列表时只做两件事：**认出这是哪句话**、**想起自己当时说了什么**。颜色用边框颜色本身就已经表达了，再写一个「黄色」是同一个信息说两遍。

**怎么做**：整条批注 = 一个用该颜色描边的框 + 原文 + 批注内容。段号、时间、颜色名一律去掉。颜色的表达靠**框的颜色**，不靠文字。

### 点击位置要跟着语义走，别另加按钮

**现象**：每条批注旁边挂一个铅笔图标才能编辑，用户得先发现它、再对准它点。

**为什么**：图标是「额外的一步」，而且在一屏多条批注时会形成视觉噪音（每个卡片右边都挂一排小图标）。

**怎么做**：

- **点原文部分 → 跳到正文里的对应位置**（用户看到句子，第一反应是「它在正文哪儿」）。
- **点批注文字部分 → 就地展开编辑**（用户看到自己写的话，第一反应是「我要改一句」）。
- 用 `title` 属性给这两种点击一句提示就够了，不要额外的图标。
- 编辑态用「失焦即保存退出」而不是「保存 / 取消」两个按钮 —— 一屏能改五条，不用五次确认。

### 批量操作靠勾选，不靠「选择模式」

**怎么做**：每条左侧一个 checkbox，勾上的进批量栏（批量删除等）。**单条删除仍然单独给一个入口**（比如 hover 才出现的 `×`）—— 只是想删一条时，不该被逼进「多选」的心智模型。

---

## AI 输出

### 任何一次 AI 调用都必须能停

**现象**：点完「问 AI」只能干等，模型跑歪了也没有退路，用户只能刷新页面。

**为什么**：AI 调用天然慢（几十秒到几分钟），而不确定性能被接受的前提是**随时可以退出**。没有停止入口，用户会不敢点。

**怎么做**：

- 前端发请求时挂 `AbortController`，停止时 `abort()` —— 立刻断开对后端输出的接收，UI 回到可操作状态。
- **顺手通知后端取消**（能取消就取消）：后端任务通常有对应的取消接口（如取消一次 run），省下的是真金白银的额度。
- **后端取消是尽力而为**：取消失败了不要报错、不要影响前端的停止 —— 用户要的是「这边停下来」，不是「后端必须收到通知」。
- 停止后给一句轻量反馈（`已停止`），**不要报成失败**。用户主动停的不叫错误。
- 监听 abort 的句柄一定要在任务结束时摘掉（`{ once: true }` + `finally` 里移除），否则 UI 状态清理会误伤下一次调用。

### 快捷键按钮显示的是名字，不是描述

**现象**：一排快捷操作按钮，其中一个宽得离谱、把别的挤歪了。

**为什么**：定义快捷键时把「描述」写进了「名称」字段（比如把整句操作说明当成按钮文案），渲染出来自然是个长句。

**怎么做**：名称**短到一个中文词组**（4~8 字），完整描述放进 `title` 悬浮提示。渲染侧额外加 `truncate` + `max-w` 兜底 —— 数据脏了也不至于把布局撑破。读配置时还可以按形态判据兜一层（过长或含句号/感叹号/分号的，判为描述而非名称）。

---

## 引用 / 标注这类「短标记」

### 标记要显示成人的读数，源码里留着机器认的东西

**现象**：正文里满屏 `[@doi:10.1021/jacs.3c07992]`，一屏看下来完全没法读。

**为什么**：这个标记是给转换链路用的（认 DOI → 换 `\cite{key}`），但它是**源码**，不是**读物**。两者要的东西不一样，不能只满足一边。

**怎么做**：显示层换成普通编号 `[12]`（按首次出现顺序，与侧栏的 `#N` 一致），源码一个字都不动。要点：

- **不要改 DOM 里的文字** —— 所见即所得编辑器普遍是「把 DOM 反序列化回 markdown」的，你塞进去的可见文字会被写回文件，把用户稿子里的标记改掉。用**伪元素** `::after { content: attr(data-num) }` 显示编号：伪元素不进 DOM，永远不会被序列化。
- 原文用一层壳包住、字体设为 0 藏起来（**仍在 DOM、仍在选区里**，所以还能选中、能删）。
- 加 `title` 让鼠标悬停能看到原始标记，用户需要时找得回来。

### 一个东西被引了 N 处，就给 N 个可以点的点

**现象**：同一篇文献在正文里引了 3 次，侧栏只给一个「跳转」按钮 —— 用户想知道「都引在哪了」，只能反复点同一个按钮碰运气。

**为什么**：列表项按「文献」聚合（这是对的，同一篇文献不该出现 3 条），但「引在哪」是**位置**信息，被聚合丢掉了。

**怎么做**：

- 每条文献**右侧**（卡片内的空白处，不占正文宽度）竖着排一列小点，**一处一个点**，数量 = 引用次数。
- 点一个 → 跳到正文那一处，**这个点变色**（表示「我现在在看这一处」）。
- 再点同一个 → 颜色还原，**并去正文里把那块高亮收掉**。这是一个干净的开关，不是「再跳一次」。

### 「高亮」一定要能被收掉

**现象**：点跳转之后，正文里那块内容一直被描边框着，怎么都去不掉。

**为什么**：高亮是「进入时设上、延时后还原」的写法。连着触发两次时，第二次记下的「原值」已经是第一次设上的描边 —— 延时回调把描边恢复成描边，就永久留下了。

**怎么做**：全局只允许**一个**高亮存在。设新高亮前先把上一个还原（清掉它的定时器 + 恢复样式）；并对外暴露一个「取消高亮」的入口，让上层能做开关语义。

### 两套 UI 画同一件事时，删掉一套

**现象**：工具栏中间有一个「插入引用」，编辑器头部又有一个「书本图标 + 引用」，长得不一样、做的是同一件事。

**为什么**：两次迭代各加了一个入口，谁也没删谁。用户看到两个不同的东西，会以为功能不同。

**怎么做**：同一份能力只留**一个**入口，位置选「用户已经习惯的那处」。多出来的那个不是「备用」，是**干扰** —— 图标还得挑个不一样的，否则更像两个功能。

---

## 长文档 / 大列表

### 工具条要固定在底部，不能让用户滚下去找

**现象**：公式编辑器里「结构」按钮排在面板最下面，每次想插一个分数都要先滚到底，插完再滚回来。

**为什么**：按「从上到下：输入 → 预览 → 工具 → 确认」排的，看着顺，用起来是每步一次长滚。

**怎么做**：**高频的插入类工具做成固定在容器底部的工具条**，分类用一排小 chip 切换、当前分类的按钮直接铺在上面。判据：如果某个按钮在一段操作里会被点 3 次以上，它就不该需要滚动才能点到。

### 专业语法是「高级」，不是入场券

**现象**：想插一个公式，必须先会写 LaTeX。

**为什么**：实现上最省事（一个 textarea 就完事），但它把「会不会写 LaTeX」变成了使用门槛 —— 大多数人更熟的是 Word，Word 的公式是**所见即所得**：点符号、在公式里直接打字。

**怎么做**：

- 把公式做成**可编辑的视觉树**：分数是真的上下两格、根号真的带根号，点进哪一格就能在哪一格打字（只有根节点 contenteditable，构件是普通内联元素 + CSS —— **不要嵌套 contenteditable**，嵌套编辑宿主的光标行为最不可靠）。
- 认不出来的 LaTeX 一律存成「渲染出来但不可编辑」的芯片，**原样存回**。这样「读进来 → 改一下 → 写回去」是无损往返的，用户原有的复杂公式不会被你悄悄改写。
- 源码框留着，但**默认收起**，标注「会写 LaTeX 才需要」。它不是入口，是后门。

### 工具条按钮要「插到光标处」，不是「追加到末尾」

**怎么做**：点符号/结构时先看焦点在哪 —— 在源码框里就插到它的光标处，否则插到视觉区的光标处，并把光标送进新结构的第一个空格子。**选中了内容要先包起来**（选中 x 点分数 → x 成为分子），这是 Word 的行为，用户不需要额外学。

### 危险动作要显式，别藏在「点一下」里

**现象**：点一条公式的预览就直接插进正文 —— 用户只是想看一眼，结果稿子里多了一段。

**为什么**：把「查看」和「写入」绑在同一个动作上了，而写入是不可见的副作用。

**怎么做**：

- 查看类元素（预览、卡片）**只负责看**；写入类动作单独给按钮，用大家公认的图标（复制/粘贴、加号、垃圾桶）。
- 「替换一处」和「替换全部」是**两个不同的按钮**，不要用一个开关去切；并且用图形区分（笔右下角加个 `1` = 只改一处，光笔 = 全改）。
- 破坏性动作（删除）配复选框做批量 —— 单条删除另外给一个入口，别逼用户先进「多选模式」。

---

## 布局联动

- 左右分栏时，**点一栏里的东西要跳到另一栏**，而不是把当前栏替换掉。用户正在看的那一栏一旦被顶掉，他得重新把它调回来 —— 这个来回成本会让人放弃使用联动。
- 判断规则：操作发生时，先看目标内容是否已经在某一栏可见；已在就用它；不在才**去改另一栏**，绝不动用户当前所在的那一栏。
- 同一份能力给两栏共用同一个实现，别为左右各写一套 —— 行为一旦不一致，用户就会觉得「有时候行有时候不行」。

---

## 文案

- **解释要短**。用户不是傻瓜，尤其不要解释「保存」按钮会保存这种事的原理。
- 该说的一句话说完：把「为什么这么设计」压成一行小字放在选项下方，或者干脆只说结论。
- 面向开发者的实现细节（写到哪个文件、走哪个接口、内部代号）**不要出现在用户界面上**。它们该出现在文档里，不是设置页里。

### 注释不是功能：把功能做直观，别用一大段说明遮住它

**现象**：一个「上传识别」按钮下面挂着三行小字，解释要先在设置里填哪个令牌、为什么要走后端、结果务必核对……

**为什么**：写的时候知道这个功能有哪些坑，就顺手把坑写在了旁边。但**用户不会读说明书**，他只是想要「点这个按钮，把公式弄进来」；一屏说明只会让他觉得这东西很麻烦。

**怎么做**：

- **先把功能本身做直观**（按钮文案直接说做什么、默认值选好、失败时在失败的地方给一句话），再考虑要不要补说明。
- 说明只在**出错的那一刻**出现（toast / 行内错误），并且只说「怎么解决」。
- 判断标准：如果一段说明是「为了解释为什么这个按钮不好用」而存在的，那要改的是按钮，不是文案。
- 面向开发者的前置条件（去哪个设置页填什么）**最多一行、并且做成可点的入口**；写不进去就别写。

---

## 学习 / 练习类界面

### 双语材料必须成对给：给了英文就要给中文

**现象**：单词卡上只有英文例句和英文解释，中文那一半要么没有、要么只存在内存里刷新就丢。

**为什么**：数据是从英文原文里抽的，英文天然就有；中文要靠额外一次翻译，容易「先不做」。但对**刚开始学的人**，只给英文等于没给 —— 他连句子都没读懂，谈不上学这个词。

**怎么做**：

- 卡片上**每一块英文内容都必须配一块中文**：英文解释 ↔ 中文解释，英文例句 ↔ 中文例句。
- **中文那一半要落盘**（进 CSV/库），不能只放在运行时状态里 —— 否则刷新一次就没了，用户永远补不齐。
- 补齐的时机放在**抽取阶段**（抽英文的时候顺手要译文），不要指望用户自己补。
- 真缺了就**明确写出来**（「缺中文译文」），别静默不显示 —— 用户看不到缺口，就永远不知道要补。
- 注意区分**短释义**和**长解释**：前者是答题选项（一个词或短语），后者是给人读懂的一句话。两者不能互相赋值。

### 出声和不出声是两个模式，不是一个小开关

**现象**：原来只有一个「朗读发音」勾选框 —— 一个复选框同时承担了"要不要声音"和"哪些题型能用"两件事，用户看不出它们是同一件事的两面。

**为什么**：听力类题型（听音选中文、中文选读音）**没有声音就根本没法作答**。把「静音」当成一个可勾可不勾的偏好，会出现"勾了听音题却静音"的自相矛盾配置。

**怎么做**：

- 把它做成**两个并列的模式按钮**（语音模式 / 静音模式），并在旁边一句说清差别。
- 静音模式下，依赖发音的题型**灰掉不可勾**（写明「需语音」），但**已勾选的保留** —— 切回语音模式立即恢复，不逼用户重勾一遍。
- 会话内部统一以「当前模式下真正可用的题型」为准（`availableTypes`）：出题、推进、轮次显示、掌握判定都用同一份，静音剔掉题型后下标不会错位。
- 语音模式下的**连读要落在"展示卡片"这一步**：先读单词、停顿一下再读例句（双管齐下才记得牢）。**答题前不要读例句** —— 挖空题的答案就在那句话里。
- 朗读多段要用 `onend` 一段接一段地串，别一口气 `speak()` 多段：浏览器对排队语义不一致，后一段的 `cancel` 会把前一段掐掉。
- 换题 / 退出会话 / 离开页面都要**掐掉当前朗读**，别让上一题的声音跟着人走。

### 只给英文题面配喇叭，中文题面别硬读

**现象**：题面是中文释义（中选英）或中文定义（定义选英）时，旁边也挂着一个"朗读"喇叭，点下去是用英文音库读中文 —— 出来的是一串乱音。

**为什么**：喇叭的语义是"把这段**英文**念给你听"。题面是中文时它没有意义；对「中选读音」这种题更是有害 —— 那道题的正确玩法是**逐个试听选项**，题面再来一个喇叭只会让人以为点它就对了。

**怎么做**：

- 只有**题面本身就是英文单词**的题型才配题面喇叭。
- 需要"听"的题型（中选读音）把喇叭挂在**每个选项外面**（跟选项按钮并列，不是套在里面）—— 套在里面的喇叭会被点成"选了这一项"直接提交。
- 听音选中文（只听音、不显示词）**题面不放文字**：写了单词就等于把答案给人看；放一个大喇叭 + 一句"可重听"即可。

### 词根词缀：拆得开才拆，拆了就摆出"怎么拼起来"

**现象**：化学专业英语大量靠前后缀构词，卡片上只给一个整词，学习者看不出它是由哪几块拼的。

**为什么**：词根词缀的价值在于**迁移**（认识 `photo-` 以后见到 `photolysis` 不用查）。但要它有用，切分必须**真的能拼回原词**。

**怎么做**：

- **硬约束**：各段按顺序拼起来必须正好等于原词，拼不回来就整组丢弃（前端 `isValidMorphemeSplit`、后端 runner 同一口径）。缺字母的切分比不切更误导人。
- **拆不出就留空**，绝不硬拆。卡片上"没有词根词缀这一块"是正常状态，不是缺数据。
- 展示时给的是**连接形式**：`photo + catalys + is`，每段下面标出类型（前缀/词根/后缀/连接）与含义，用户一眼看到"它是拼出来的"。
- 切分一旦用于出题就有**第二重身份**：拼写题的"块"就是这些词素；切不出来才退化成逐字母。
- **人工修正不是常驻入口**：切分由 AI 在抽取时一起产出，改它是补锅 —— 挂在该文献的「编辑文献」弹窗里（跟改元数据同一个地方），不要做成一个随时待命的编辑态。

### 一组学完直接进下一组，别用总结页打断心流

**现象**：每学完一组就弹一页「本组学习完成 · 共 N 词 · 答对 M 次」，要点「再来一组」才继续 —— 每 5 个词被打断一次。

**为什么**：总结页的信息量（答对/答错/新掌握）对**当下这一秒**没有用，却强制把用户从"做题节奏"里拽出来。学习的心流比统计数字值钱。

**怎么做**：

- 一组走完（所有题型轮完）**直接起下一组**，不弹任何东西。
- 一路接着学，直到**当天没有可学的词**（学习模式：`learning` / `new` 都空了；复习模式：没有到期的词）才展示完成页 —— 那时的总结是"收工"的收尾，不是打断。
- 自动续组要在**这次渲染提交之后**再挑下一组（用 effect + 最新的词表），否则刚学完的词在旧快照里还是 `learning`，会把同一组反复挑出来；中间渲染一个「继续下一组…」占位，避免闪一下首页。
- 起不来的原因是"确实没有可学的词"，与用户手点开始时的"暂无新词"不是一回事：前者静默落到完成页，不要弹错误 toast。

### 做题前不给答案，低分才复盘

**现象**：翻译/摘要练习一翻开就把参考译文、踩分点、难点全摊在页面上，等于告诉用户"不用做，答案在这"，题目被直接跳过。

**为什么**：练习的价值在**先自己产出一遍**。把判分标准提前亮出来，用户就会照着抄，练不到任何东西。

**怎么做**：

- 做题前只给**题面 + 作答框 + 提交**。参考译文、踩分点、难点一律不进这一屏。
- 判分后：先给分数与命中/漏掉的点（即时反馈）。**得分低于低分线**才弹出「学习卡片」复盘（弹窗），高分直接过，不打断节奏。
- 低分线（`translationLowScore`）放**设置**里让用户自己定（50-95，默认 70），别写死。
- 卡片里必须有：原文、参考译文、难点、踩分点（逐条标出这次命中/漏掉）、**每个踩分点漏掉扣多少分**、以及一句「应该怎么做」。踩分点在卡片里可编辑。
- **不要在这一屏加「标记掌握/标记完成」**：那是用户的自我评价按钮，与"做题—判分—复盘"这条主线无关，只会让人以为必须点它才算过。
- 更新历史的判分记录（上次漏掉的点、上次反馈）也归到卡片里 —— 放在做题前同样等于泄题。

### 摘要题的前提是"摘要在任何一次写入后都还在"

**现象**：DOI 快捷入库的文献做不了摘要翻译题；本来有摘要的文献，在管理页编辑保存一次之后摘要就没了。

**为什么**：摘要来自元数据（Crossref / OpenAlex），是摘要翻译练习的**题面与参考答案来源**。真正的 bug 不在练习页，而在**读写链路把它抹掉了** —— 中间层对象没有摘要字段，保存时按空串写回 CSV。

**怎么做**：

- 展示层对象（如 `Paper`）必须显式携带摘要字段，`literatureToPaper` / `paperToLiterature` 双向映射，**不许在映射里写死空串**。
- Crossref 的 `abstract` 是**可选字段**（不少出版社不提交），所以入库时要再用 OpenAlex 的 `abstract_inverted_index` 批量兜一次，并写回本地缓存。
- 只靠 DOI 元数据入库的文献通常**只有英文摘要**。这时不能让整道题消失：英译中照样出（中文参考答案由 AI 在生成时补一份 `reference_translation`），中译英因缺中文题面而跳过 —— **有哪边出哪边**。
- 摘要在管理页的文献弹窗里可看可改（英文/中文两个输入框），作为人工补录的兜底入口。

### 期刊名显示缩写，DOI 只值一个复制键

**现象**：文献列表里期刊名一长串、DOI 原文占一列 —— 信息都在，但都在挤占横向空间，用户真正要的动作（复制 DOI）反而要精确定位那一小段文本。

**为什么**：列表里期刊是**用来识别**的，缩写（JACS / CSR）就够，全名是详情页的事。DOI 更是：没人关心它的字面长什么样，只关心能不能复制走。

**怎么做**：

- 期刊默认按启发式取首字母缩写（丢 of/the/and 等虚词，保留 Journal/Review 的首字母），用户可用铅笔按钮**手工覆盖**，覆盖表本地存起来后一直沿用（`journal_abbreviations`）。
- DOI **不显示文本**，只给一个图标复制按钮，不做任何说明文案。
- 作者只留**一作**与**通讯**：一作无星号、通讯带 ★，各占一行；通讯优先取作者串里带 `*` 的那位，没有标记就按惯例取最后一位，只有一个作者时不显示通讯行。
- 分类一行一个、按下标交替上色，让"这篇属于哪几类"一眼数得清。

### 列表形态在一个页面里只能有一种

同 [两套 UI 画同一件事时，删掉一套](#两套-ui-画同一件事时删掉一套)：管理页的文献库原来有「表格视图 / 卡片视图」两套，图书库/期刊模板又是网格卡片，其他文档却是横条 —— 同一个页面里三种形态，用户每次换 Tab 都要重新认界面。

统一为**横条列表**（`divide-y` + `flex` 行）：一条数据一行，能上下滚，尽量不左右滚。**图书不上传封面**，所以图书/文档/期刊模板一律不留封面位（用固定尺寸图标块代替）；文献的题图是真实存在的信息，保留缩略图并支持点开大图。
